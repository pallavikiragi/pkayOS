export function TrashWindow() {
  return (
    <div className="trash-window">
      <p className="doc-body">TRASH IS EMPTY.</p>
      <p className="doc-body doc-body--muted">
        Discarded drafts and deprecated explorations are purged from the
        workstation. Nothing to recover.
      </p>
    </div>
  );
}
